{
  "name": "@stout/electron",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsup src/main.ts --format esm --clean --external electron --external @stout/server --external @stout/core",
    "start": "electron .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stout/core": "workspace:*",
    "@stout/server": "workspace:*"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.5"
  }
}
